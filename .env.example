# Telegram bot token from @BotFather (required)
BOT_TOKEN=

# Comma-separated Telegram numeric user IDs allowed to create/approve tasks and review submissions.
# Find your ID by messaging @userinfobot. Example: ADMIN_IDS=12345678,87654321
ADMIN_IDS=

# PostgreSQL connection string (required). Railway injects this from its Postgres
# plugin. For local dev, `docker compose up -d` starts one:
DATABASE_URL=postgresql://multiagency:multiagency@localhost:5455/multiagency

# Optional chat where newly opened tasks are announced — the primary public-discovery
# surface. Any chat works: a private/public group or a broadcast channel (numeric id like
# -1001234567890, or @publicname). The bot must be a member (an admin with "Post Messages"
# for a channel). The post's Apply button deep-links when BOT_USERNAME is set (below),
# otherwise it just points at /open; leave empty to disable (approval is unaffected).
ANNOUNCE_CHAT_ID=

# Bot @username without the @ (optional). When set, the announcement post carries a
# deep-link "Apply" button (t.me/<username>?start=t<taskId>) instead of only a /open hint.
BOT_USERNAME=

# Global cap on outbound notifications per second, applied across the whole queue by the
# single delivery worker (optional, default 25; Telegram's bulk limit is ~30/s).
NOTIFY_RATE_PER_SEC=25

# Max pending (undecided) applications one contributor may hold at once (optional, default 5).
MAX_OPEN_APPLICATIONS=5

# NEAR AI Cloud API key for AI assistance (optional — AI features degrade gracefully if unset).
# Generate one at https://cloud.near.ai
NEAR_AI_API_KEY=

# NEAR AI Cloud base URL (optional, OpenAI-compatible endpoint)
NEAR_AI_BASE_URL=https://cloud-api.near.ai/v1

# Model used for AI assistance (optional). See https://cloud-api.near.ai/v1/models for options.
# Prefer NEAR-native private-TEE chat models. Avoid reasoning models (gpt-oss, qwen3.x) —
# they may return empty content for short completions. Alternative: google/gemma-4-31B-it
AI_MODEL=deepseek-ai/DeepSeek-V4-Flash

# Model for the conversational agent (group /ai mode). Needs reliable tool
# calling, so it defaults to a stronger model than AI_MODEL — DeepSeek-Flash
# emits malformed tool arguments. Any tool-capable model on the endpoint works.
AGENT_MODEL=anthropic/claude-haiku-4-5

# Signal detection (per-group opt-in via /enablesignals; needs NEAR_AI_API_KEY).
# Minimum AI score (0-10) a group message must reach to become a Draft task (default 6).
SIGNAL_SCORE_THRESHOLD=6

# Max AI evaluations per room per hour — bounds the AI bill of a flooded group (default 20).
SIGNAL_MAX_PER_HOUR=20

# Max conversational-agent turns per room per hour (group /ai mode) — the agent
# runs on the stronger AGENT_MODEL, so this bounds what a mention flood can spend (default 20).
AGENT_MAX_PER_HOUR=20

# Global (all rooms combined) hourly caps — the actual ceiling on the AI bill:
# the per-room caps scale linearly with rooms, and any group join registers a
# room (defaults 200 each).
SIGNAL_GLOBAL_MAX_PER_HOUR=200
AGENT_GLOBAL_MAX_PER_HOUR=200

# Days an Assigned task can sit with no submission activity before /admin counts
# it as stale — the claim-and-abandon surface (default 7).
STALE_ASSIGNED_DAYS=7

# ---- Mini App web tier (optional — the web server stays off with no port) ----

# Port for the in-process web server (Mini App + read-only oRPC API). Railway
# injects PORT automatically; WEB_PORT is the local-dev override. Unset = off.
WEB_PORT=

# Public HTTPS origin the Mini App is served from (e.g. https://app.example.com).
# When set, the Telegram chat-menu and home-menu "board" buttons point at it.
WEB_APP_URL=

# ---- NEAR payouts (optional — the DAO payout path stays dormant without them) ----

# The NEAR network payouts settle on.
NEAR_NETWORK=testnet

# Sputnik DAO the push-payout model settles through (add_proposal Transfer,
# council-approved — see PAYOUTS.md). This id drives read-only reconciliation; the
# add_proposal WRITE is signed via OUTLAYER below (or an admin's own wallet), never
# with a fund-moving key.
DAO_CONTRACT_ID=

# OutLayer agent custody (fastnear.com) — the bot's DAO-Requestor wallet whose key
# lives in a TEE. The bot holds only this API key (never a fund-moving key) and uses
# it ONLY to submit add_proposal — the bot's single proposer path. Empty → /pay
# refuses with guidance (no printed-command fallback: a replayable command was the
# root of the duplicate-proposal hazard). OUTLAYER_BASE_URL derives from
# NEAR_NETWORK when unset. See docs/outlayer-setup.md.
OUTLAYER_API_KEY=
OUTLAYER_BASE_URL=

# URL template for viewing a DAO proposal in a governance UI (the operator's
# dashboard, Astro, etc.) — `{id}` is replaced by the proposal id. Powers the
# verify-before-voting deep links on /pay and /payouts; empty shows plain ids.
# Example: https://dash.example.com/proposals/{id}
DAO_PROPOSAL_URL=

# Where a confused user (or a payout question) reaches a human — an @handle,
# URL, or email, surfaced in /help, /privacy, /terms, and the contributor
# payout view. Empty hides the lines; set it before a public launch.
SUPPORT_CONTACT=
